# Main requirements file for Vercel deployment
# Vercel looks for requirements.txt in root directory

flask==3.0.0
flask-cors==4.0.0
flask-jwt-extended==4.6.0
werkzeug==3.0.1
python-dotenv==1.0.0
gunicorn==21.2.0
